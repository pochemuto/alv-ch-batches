---
spring:
  profiles:
    # project convention: shared settings are defined config/application-test.yml
    active: test

ch:
  alv:

    batches:
      chunkSizes:
        company: 25

    company:
      download_dir: /tmp # c:/temp
      ftp:
        host: localhost
        port: 8002
        clientMode: 0 # 0=local_active 2=local_passive
        remoteVerificationEnabled: true
        username: ftpdev
        password: ftpdev
        filename: AVAMPSTS.xml
