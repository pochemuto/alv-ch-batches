---
spring:
  profiles:
    # project convention: shared settings are defined /test/maven/config/...
    active: test

ch:
  alv:
    batches:
      chunkSizes:
        prospective: 20

    partnerjob:
      sources:

        partner1:
          name: "crazy white-on-red-cross jobs"
          mode: HTTP
          uri:  http://localhost:8180/partner1_feed.xml

        partner2:
          name: "awesome yellow jobs"
          mode: HTTP
          uri:  http://localhost:8180/partner2_feed.xml
