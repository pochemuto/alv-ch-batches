---
spring:
  profiles:
    # project convention: shared settings are defined /test/maven/config/...
    active: test

ch:
  alv:
    batches:
      chunkSizes:
        partnerjob: 2

    partnerjob:
      sources:

        ubs:
          mode: ubs
          uri:  src/test/resources/ubs_feed.xml

        partner1:
          mode: prospective
          uri:  http://localhost:8180/partner1_feed.xml

        partner2:
          mode: prospective
          uri:  http://localhost:8180/partner2_feed.xml
