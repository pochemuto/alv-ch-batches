spring:
  profiles:
    active: test
  batch:
    job:
      #names: use job names if some shall run at system startup
      enabled: true # means that no job will be started on system startup

ch:
  alv:
    batch:
      companydata:
        avgfirmen:
          url: ftp://ftpdev:ftpdev@localhost/AVAMPSTS.xml
