---
spring:
  profiles:
    # project convention: shared settings are defined config/application-test.yml
    active: test

ch:
  alv:

    batches:
      chunkSizes:
        job:      1000
        location:  500

    jobdesk:
      elasticsearch:
        index: test_jobdesk
